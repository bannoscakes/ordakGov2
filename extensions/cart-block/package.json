{
  "name": "@ordakgov2/cart-block",
  "version": "0.1.0",
  "private": true,
  "description": "Ordak Go theme app extension — cart-page and cart-drawer scheduling block",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "size": "gzip -c assets/cart-scheduler.js | wc -c"
  },
  "dependencies": {
    "preact": "10.25.4",
    "@preact/signals": "1.3.2"
  },
  "devDependencies": {
    "esbuild": "0.24.2",
    "typescript": "5.7.3"
  }
}
