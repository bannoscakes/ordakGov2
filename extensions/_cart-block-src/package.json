{
  "name": "@ordakgov2/cart-block-src",
  "version": "0.1.0",
  "private": true,
  "description": "Source + build pipeline for the cart-block theme app extension. Outputs to ../cart-block/assets/ which is what Shopify deploys.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "size": "gzip -c ../cart-block/assets/cart-scheduler.js | wc -c"
  },
  "dependencies": {
    "preact": "10.25.4",
    "@preact/signals": "1.3.2"
  },
  "devDependencies": {
    "esbuild": "0.24.2",
    "typescript": "5.7.3"
  }
}
